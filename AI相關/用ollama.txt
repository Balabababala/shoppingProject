ollama pull qwen2:7b or qwen2:1.5b

ollama serve


test 他的能力

curl -X POST http://localhost:11434/api/generate -H "Content-Type: application/json" -d "{\"model\": \"qwen2:7b\", \"prompt\": \"審核以下評論：『這產品很棒，值得購買！』請返回 JSON 格式，包含 status (APPROVED 或 REJECTED)、reason (原因，使用繁體中文，必須提供具體說明，例如為何批准或拒絕) 和 sentiment (POSITIVE、NEGATIVE 或 NEUTRAL)。確保回應為有效的 JSON 格式。\",\"stream\": false}"

spring AI 的
dependcy

<dependency>
	<groupId>org.springframework.ai</groupId>
	<artifactId>spring-ai-ollama</artifactId>
	<version>1.0.0</version> <!-- 請檢查最新版本 -->
</dependency>
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-web</artifactId>
</dependency>
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-data-jpa</artifactId>
</dependency>
<dependency>
    <groupId>com.fasterxml.jackson.core</groupId>
    <artifactId>jackson-databind</artifactId>
</dependency>

application.properties

spring.ai.ollama.base-url=http://localhost:11434
spring.ai.ollama.chat.model=qwen2:7b							//看情況改
spring.ai.ollama.chat.options.temperature=0.7
spring.ai.ollama.chat.options.top-p=0.9



test用

curl -X POST http://localhost:8080/api/reviews/review-all